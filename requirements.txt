fastapi
uvicorn
torch>=2.0.0
redis
pydantic
prometheus_client
# milvus
numpy<2.0.0
faiss-gpu
hdbscan
pyjwt
kubernetes
httpx

# LoRA/PEFT関連
peft
accelerate

pymilvus

# 省略可能な既存パッケージ類はコメント扱い
# ...
# ...
# そのほか既存の記述は省略

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      SKIP_HF: "true"
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.10
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest --maxfail=1 --disable-warnings -q