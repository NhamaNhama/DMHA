fastapi
uvicorn
torch>=2.0.0
redis
pydantic
prometheus_client
# milvus
numpy<2.0.0
faiss-gpu
hdbscan
pyjwt
kubernetes
httpx

# LoRA/PEFT関連
peft
accelerate

pymilvus

# 省略可能な既存パッケージ類はコメント扱い
# ...
# ...
# そのほか既存の記述は省略

@patch("transformers.AutoModelForCausalLM.from_pretrained", return_value=MagicMock())
@patch("pymilvus.connections.connect", return_value=MagicMock())
def test_contextualize_endpoint(mock_milvus, mock_hf):
    # ここで ContextAwareSystem() を生成しても from_pretrained はモックされる
    mock_system = MockSystem()